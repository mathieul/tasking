- wiselinks_title page_title
%body
  = render "layouts/navigation_bar"
  %header.title
    .summary-bar.container
      .row
        %div{page_width(:title)}
          %h1= page_title
        %div{page_width(:description)}
          = flash_messages
          = yield(:page_description)

  .main-content{page_container}
    %br
    = yield

  = yield :javascript
